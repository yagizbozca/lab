apiVersion: v1
kind: Service
metadata:
  labels:
    app: mealie-service-label
  name: mealie-service
  namespace: mealie-namespace
spec:
  ports:
  - port: 9000
    protocol: TCP
    targetPort: 9000
  selector:
    app: mealie-deployment-selector
  type: LoadBalancer
